package mano;

public class maintest {

	public static void main(String[] args) {
		
		
	    String anything=Thread.currentThread().getName(); //it is used to get name of current running thread
	    //i.e Thread.currentThread()--is used to know current running method.
	    System.out.println("The name of current running thread is:    "+anything);
	   
    }
}
